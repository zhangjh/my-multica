---
title: 태스크
description: 태스크는 한 작업의 컨텍스트, 담당자, 상태, 실행 기록을 보관합니다.
---

import { Callout } from "fumadocs-ui/components/callout";

태스크는 Multica에서 작업을 계획하는 기본 단위입니다. 기능 하나, 버그 하나, 조사 하나처럼 멤버나 에이전트가 책임지고 진행해야 하는 모든 작업이 태스크가 될 수 있습니다. 관련 논의, 상태 변경, 모든 실행이 한곳에 남으므로 채팅 기록이나 터미널 로그에서 컨텍스트를 다시 조합할 필요가 없습니다.

## 태스크의 구성

| 내용 | 용도 |
|---|---|
| **제목과 설명** | 목표, 배경, 요구 사항, 인수 조건입니다. |
| **상태와 우선순위** | 작업이 어느 단계에 있고 무엇을 먼저 처리할지 나타냅니다. |
| **담당자** | 워크스페이스 멤버, 에이전트 또는 스쿼드입니다. |
| **날짜, 라벨, 사용자 지정 속성** | 계획, 분류, 팀 고유 필드입니다. |
| **프로젝트와 상하위 관계** | 더 큰 작업 범위에 포함하거나 하위 태스크로 나눕니다. |
| **활동과 실행 로그** | 댓글, 상태 변경, 실행, 에이전트가 반환한 결과입니다. |

![태스크 상세: 설명, 논의, 오른쪽 속성, 실행 로그](/images/docs/quickstart-run-result.webp)

## 태스크 만들기

**태스크** 페이지나 프로젝트에서 새 태스크를 만들고 제목을 입력합니다. 다른 속성은 언제든 보충할 수 있습니다.

각 태스크에는 `MUL-123` 같은 번호가 있습니다. 숫자는 워크스페이스 안에서 증가합니다. 관리자가 워크스페이스의 태스크 접두사를 바꾸면 번호가 새 접두사로 표시됩니다. 자세한 내용은 [워크스페이스](/workspaces#워크스페이스-만들기)를 참고하세요.

## 담당자 선택

| 담당자 | 결과 |
|---|---|
| **멤버** | 해당 멤버가 후속 작업을 책임지며 실행은 생성되지 않습니다. |
| **에이전트** | 해당 에이전트의 실행이 생성됩니다. |
| **스쿼드** | 스쿼드 리더가 먼저 받은 뒤 누구에게 맡길지 결정합니다. |

에이전트나 스쿼드에 할당할 때 태스크가 `backlog` 상태가 아니면 즉시 큐에 들어갑니다. 런타임이 오프라인이면 실행은 큐에서 기다립니다. 보관된 에이전트와 스쿼드는 할당할 수 없습니다.

할당은 에이전트의 접근 범위(Access)를 우회하지 않습니다. 스쿼드에 할당하면 리더의 Access를 확인합니다. 자세한 내용은 [태스크를 에이전트에게 할당하기](/assigning-issues)를 참고하세요.

## 상태

모든 워크스페이스에는 기본 상태 7개가 준비되어 있고, 이들은 4개의 수명 주기 카테고리로 묶입니다.

| 카테고리 | 기본 상태 | 의미 |
|---|---|---|
| `unstarted` | `backlog`, `todo` | 아직 시작하지 않은 작업. 계획된 것이든 미뤄둔 것이든 같습니다. |
| `started` | `in_progress`, `in_review`, `blocked` | 이미 시작했거나, 검토를 기다리거나, 지금은 진행할 수 없는 상태. |
| `done` | `done` | 성공적으로 끝난 종료 상태. |
| `closed` | `cancelled` | 더 이상 진행하지 않는 종료 상태이며, 성공적인 완료는 아닙니다. |

카테고리는 수명 주기 판단을 단순하게 유지하기 위한 것입니다. 보드와 목록, 필터, 정렬은 모두 개별 상태를 기준으로 동작하며, 구체적인 상태에는 에이전트와 자동화가 의존하는 더 세밀한 동작이 남아 있습니다.

| 상태 | 의미 |
|---|---|
| `backlog` | 지금은 시작하지 않습니다. 에이전트에게 할당된 태스크도 `backlog`에서 벗어나야 실행이 생성됩니다. |
| `todo` | 작업이 명확하며 시작을 기다립니다. |
| `in_progress` | 처리 중입니다. |
| `in_review` | 결과가 있으며 검토를 기다립니다. |
| `done` | 완료되었습니다. |
| `blocked` | 지금은 계속 진행할 수 없습니다. |
| `cancelled` | 더 이상 진행하지 않으며 기록은 유지합니다. |

상태 사이에는 고정된 전환 순서가 없으며 멤버와 에이전트 모두 상태를 직접 변경할 수 있습니다.

에이전트는 작업이 태스크에 실제로 일으킨 변화를 그대로 상태에 기록합니다. 태스크가 요구하는 것을 만들기 시작하면, 그것이 코드든 조사든 설계든 태스크 자체가 요청한 리뷰든 바로 `in_progress`로 옮겨 실행 중에도 보드에 드러나게 합니다. 전달하면 `in_review`로 옮기고, 작업이 이번 턴을 넘어 이어지면 `in_progress`로 유지합니다. 그 태스크 자체의 결과물을 만들지 않는 턴, 예를 들어 질문에 답하거나 다른 곳이 소유한 작업에 조언하는 경우에는 처음부터 끝까지 상태를 바꾸지 않습니다. 이 변경은 실행 중에 에이전트가 Multica CLI로 명시적으로 기록하는 것입니다. 실행이 시작되거나 끝날 때 서버가 태스크 상태를 바꾸지는 않습니다(아래 두 가지 시스템 예외 제외). `done`은 보통 사람의 확인이나, 종료 의도를 포함한 PR 병합 같은 연동으로 지정됩니다.

<Callout type="info">
다음 두 가지 변경은 시스템이 수행합니다.

- 실행이 실패하고 해당 태스크에 다른 실행이 없으며 재시도도 트리거되지 않으면 `in_progress`가 `todo`로 돌아갑니다.
- 연결된 GitHub PR이 종료 의도를 포함한 채 병합되고, 여전히 open 또는 draft 상태인 다른 연결 PR이 없으면 태스크가 `done`으로 바뀝니다.
</Callout>

### 사용자 지정 상태

워크스페이스의 owner 또는 admin은 **설정 → 태스크 상태**에서 `Code Review`, `QA`, `Rework` 같은 상태를 직접 추가할 수 있습니다. 모든 상태는 4개의 수명 주기 카테고리 중 하나에 속합니다.

| 카테고리 | 수명 주기상의 의미 |
|---|---|
| `unstarted` | 아직 시작하지 않음. 기본 상태 `backlog`와 `todo`를 포함합니다. |
| `started` | 진행 중. 검토 대기와 응답 대기도 포함합니다. |
| `done` | 성공적으로 끝난 종료 상태. |
| `closed` | 취소했거나 더 이상 진행하지 않는 종료 상태이며, 성공적인 완료는 아닙니다. |

사용자 지정 상태가 물려받는 것은 수명 주기의 의미뿐이며, 기본 상태의 개별 동작은 물려받지 않습니다. `backlog`의 보류, `in_review`의 [오토파일럿](/autopilots) 실행 마무리, `blocked`의 실패 처리, `in_progress`의 실패 복구는 모두 상속되지 않습니다. 그 동작이 필요하면 해당 기본 상태를 사용하세요. 이전에 만든 사용자 지정 상태도 같은 규칙을 따릅니다. Awaiting Response는 진행 중인 작업이며 리뷰의 자동 완료가 아닙니다. 할당과 생성은 기존과 같이 일반적인 에이전트 트리거 규칙을 따릅니다.

플랫폼이 스스로 상태를 지정할 때, 예를 들어 `todo`로 되돌리거나 병합된 PR로 `done`을 지정할 때 기록되는 것은 항상 기본 상태입니다.

이미 설치된 클라이언트를 위해 API의 기존 카테고리 필드는 7개 값의 전송용 enum을 계속 반환합니다. 새 클라이언트는 이를 4개 카테고리로 정규화하며, 전송 값이 동작 상속을 부여하는 일은 없습니다. 데이터베이스에 저장되는 값은 4개의 수명 주기 값뿐입니다.

여기에서 네 가지가 따라옵니다.

- **카테고리는 상태를 만든 뒤에는 바꿀 수 없습니다.** 나중에 바꾸면 이미 그 상태인 모든 태스크의 동작을 조용히 다시 쓰게 되므로, 편집 화면에서 읽기 전용입니다. 동작을 먼저 정하고 이름을 붙이세요.
- **상태 기준 그룹화는 개별 상태를 사용합니다.** 보드의 열, 목록의 섹션, 스윔레인의 상태 열은 기본 상태와 사용자 지정 상태를 각각 구분합니다. `Code Review`와 `QA`는 둘 다 `started`에 속하더라도 서로 다른 열이 됩니다. 카테고리는 내부적인 수명 주기 분류이며 열 이름을 대체하는 것이 아닙니다.
- **기본 상태는 잠겨 있습니다.** 이름, 색, 카테고리를 바꿀 수 없고 보관할 수도 없습니다. 이 페이지를 한 번도 열지 않은 워크스페이스의 보드는 예전과 완전히 동일합니다.
- **보관은 비활성화이지 삭제가 아닙니다.** 이미 그 상태인 태스크는 이름도 색도 동작도 그대로 유지합니다. 다음에 누군가 상태를 지정할 때 선택지로 나오지 않을 뿐입니다.

기본 상태는 인터페이스 언어로 번역되지만, 사용자 지정 상태는 입력한 이름이 그대로 표시됩니다. API와 CLI는 키로 참조합니다: `multica issue status MUL-42 code_review`. 설정 페이지는 이름에서 키를 생성하고, API는 키를 직접 지정할 수 있으며 생략했을 때만 이름에서 생성합니다. 어느 쪽이든 키는 만들 때 확정되며, 나중에 이름을 바꿔도 키는 바뀌지 않습니다.

## 태스크와 실행

태스크는 계속 유지되는 작업 기록이고 [실행](/tasks)은 에이전트가 한 번 실행한 기록입니다. 하나의 태스크에서 첫 구현, 추가 수정, 재검토처럼 여러 실행이 차례로 생성될 수 있습니다. 실행이 "완료됨"인 것은 이번 실행이 끝났다는 뜻일 뿐이며, 태스크의 완료 여부는 태스크 상태를 기준으로 판단합니다.

## 프로젝트와 하위 태스크

하나의 태스크는 최대 하나의 [프로젝트](/projects)에 속할 수 있습니다. 프로젝트는 그 안의 실행에 공유 설명과 리소스를 제공합니다. 다른 프로젝트로 옮겨도 복사본이 만들어지지 않습니다.

큰 작업은 하위 태스크로 나눌 수 있습니다. 상위 태스크에는 전체 목표를 남기고 하위 태스크는 각각 독립적으로 진행합니다. 상위와 하위 태스크의 상태는 서로 연동되지 않습니다.

하위 태스크에는 **단계**(stage)를 지정해 1, 2, 3처럼 묶어서 진행할 수 있습니다. 현재 가장 이른 미완료 단계의 모든 하위 태스크가 `done` 또는 `cancelled`가 되면 상위 태스크에 "하위 태스크 완료" 알림이 전달됩니다. 상위 태스크 담당자가 에이전트이면 에이전트가 깨어나 다음 단계로 진행할지 결정합니다. 단계를 지정하지 않은 하위 태스크는 같은 묶음으로 간주하며 모두 끝났을 때 한 번 알립니다.

## 보기 전환

태스크 페이지에서는 목록, 보드, 표, 간트 차트, 스윔레인 다섯 가지 보기를 제공합니다. 상태, 담당자, 프로젝트, 기타 속성으로 필터링하고 정렬할 수 있으며 모두 같은 태스크 집합을 표시합니다.

## 태스크 삭제

모든 워크스페이스 멤버가 태스크를 삭제할 수 있습니다.

<Callout type="warning">
삭제는 복구할 수 없습니다. 태스크와 댓글, 첨부 파일, 관련 기록이 영구적으로 제거되고 끝나지 않은 실행도 취소됩니다. 더 이상 진행하지 않으려면 상태를 `cancelled`로 바꾸세요. 논의와 결과는 계속 확인할 수 있습니다.
</Callout>

## 다음 단계

- [프로젝트](/projects) — 여러 태스크를 함께 완료해야 하는 작업을 구성합니다.
- [댓글](/comments) — 태스크를 중심으로 논의하고 답글과 @멘션을 사용합니다.
- [태스크를 에이전트에게 할당하기](/assigning-issues) — 에이전트에게 작업을 맡기고 첫 실행을 시작합니다.
- [실행](/tasks) — 큐 대기, 실행, 재시도, 취소를 알아봅니다.
